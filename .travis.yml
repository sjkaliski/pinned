language: go
go:
  - 1.8.x
  - 1.9.x
  - 1.10.x
  - tip

matrix:
  allow_failures:
    - go: tip

install:
  - go get -u golang.org/x/lint/golint
  - go get -t -v ./...

script:
  - go test -v -race ./...
  - go vet ./...
  - golint -set_exit_status ./...

notifications:
  email: false
